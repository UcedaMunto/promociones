<form method="post" action="{{ path('comentario_reunion_delete', {'id': comentario_reunion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comentario_reunion.id) }}">
    <button class="btn">Delete</button>
</form>
