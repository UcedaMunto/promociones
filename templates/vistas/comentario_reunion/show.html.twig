{% extends 'base.html.twig' %}

{% block title %}ComentarioReunion{% endblock %}

{% block body %}
    <h1>ComentarioReunion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comentario_reunion.id }}</td>
            </tr>
            <tr>
                <th>Mensaje</th>
                <td>{{ comentario_reunion.mensaje }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ comentario_reunion.fecha ? comentario_reunion.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TituloComent</th>
                <td>{{ comentario_reunion.tituloComent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comentario_reunion_index') }}">back to list</a>

    <a href="{{ path('comentario_reunion_edit', {'id': comentario_reunion.id}) }}">edit</a>

    {{ include('comentario_reunion/_delete_form.html.twig') }}
{% endblock %}
