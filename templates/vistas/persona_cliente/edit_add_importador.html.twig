{% extends 'base.html.twig' %}

{% block title %}Edit PersonaCliente{% endblock %}

{% block body %}
 
    <a href="{{ path('persona_cliente_index') }}">back to list</a>

    <div class="col-md-12">
            <!-- Basic registration form-->
            <div class="card shadow-lg border-0 rounded-lg mt-1">
                <div class="card-header justify-content-center"><h3 class="font-weight-light my-4">Edición de usuarios</h3></div>
                <div class="card-body">
                    <!-- Registration form-->
                    {% if mensajes['0']  %}
                        <div class="alert alert-danger" role="alert">
                        <strong>{{ mensajes[0] }}</strong>  
                        </div>
                    {% endif %}


                    {{ form_start(form2) }}
                        {{ form_widget(form2) }}
                        <button class="btn">{{ button_label|default('Save') }}</button>
                    {{ form_end(form2) }}
      

                    
                </div>
                <div class="card-footer text-center">
                   
                </div>
            </div>
        </div>
        
{% endblock %}

{% block js_general %}
    <script src="{{ asset('js/general.js') }}" ></script>
{% endblock %}
