<form method="post" action="{{ path('persona_cliente_delete', {'id': persona_cliente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona_cliente.id) }}">
    <button class="btn">Delete</button>
</form>
