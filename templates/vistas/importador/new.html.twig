{% extends 'base.html.twig' %}

{% block title %}Nuevo importador{% endblock %}

{% block body %}
    <h1>Crear nuevo importador</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}

    <a href="{{ path('importador_index') }}">back to list</a>
{% endblock %}


      