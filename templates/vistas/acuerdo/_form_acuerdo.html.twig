{% if mensajes['0']!="" %}
            <div class="alert alert-danger" role="alert">
                <strong>{{ mensajes[0] }}</strong>  
            </div>
{% endif %}

    {{ form_start(form_ejecucion, { 'attr': {'id': 'form_acuerdo'}} ) }}
    <div class="FormularioReunion">
        <div class="form-row">
            <div class="form-group col-xs-48 col-sm-18 col-md-12 col-lg-12">
                    <label class="small mb-1" > Reunion: </label>
                    {{ form_widget(form_ejecucion.discusionJefatura, {'id': 'dj_add'} )}}
                    {{ form_errors(form_ejecucion.discusionJefatura) }}    
            </div> 
        </div>   

        <div class="form-row">
                
                    <label class="small mb-1" > Titlo: </label>
                    {{ form_widget(form_ejecucion.nombre) }}
                    {{ form_errors(form_ejecucion.nombre) }}    
        </div>    
        <div class="form-row">

            <div class="form-group col-xs-24 col-sm-12 col-md-6 col-lg-6">
                    <label class="small mb-1" > Supervisor: </label>
                    {{ form_widget(form_ejecucion.supervisor) }}
                    {{ form_errors(form_ejecucion.supervisor) }}    
            </div>
            <div class="form-group col-xs-24 col-sm-12 col-md-6 col-lg-6">
                    <label class="small mb-1" > Fecha de inicio acuerdo: </label>
                    {{ form_widget(form_ejecucion.fechaVigencia) }}
                    {{ form_errors(form_ejecucion.fechaVigencia) }}    
            </div>
        </div>        
        <div class="form-row">
                    <label class="small mb-1" > Descripción: </label>
                    {{ form_widget(form_ejecucion.descripcion) }}
                    {{ form_errors(form_ejecucion.descripcion) }}    
         
            <div class="form-group col-xs-48 col-sm-18 col-md-12 col-lg-12">
                    <label class="small mb-1" > Departamentos que deben cumplirlo: </label>
                    {{ form_widget(form_ejecucion.departamentEmpresa, {'id': 'dep_add'} ) }}
                    {{ form_errors(form_ejecucion.departamentEmpresa) }}    
            </div>
        </div>

        <button class="btn" id="btnAcuerdos">{{ button_label|default('Guardar') }}</button>
    </div>
    
{{ form_end(form_ejecucion) }}