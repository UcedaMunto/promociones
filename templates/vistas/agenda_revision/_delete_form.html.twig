<form method="post" action="{{ path('agenda_revision_delete', {'id': agenda_revision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agenda_revision.id) }}">
    <button class="btn">Delete</button>
</form>
