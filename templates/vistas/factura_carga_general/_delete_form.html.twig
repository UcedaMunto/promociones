<form method="post" action="{{ path('factura_carga_general_delete', {'id': factura_carga_general.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factura_carga_general.id) }}">
    <button class="btn">Delete</button>
</form>
