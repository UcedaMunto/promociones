{% extends 'base.html.twig' %}

{% block title %}Archivo{% endblock %}

{% block body %}
    <h1>Archivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ archivo.id }}</td>
            </tr>
            <tr>
                <th>Ruta</th>
                <td>{{ archivo.ruta }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('archivo_index') }}">back to list</a>

    <a href="{{ path('archivo_edit', {'id': archivo.id}) }}">edit</a>

    {{ include('archivo/_delete_form.html.twig') }}
{% endblock %}
