{% extends 'base.html.twig' %}

{% block title %}CtlTipoPromocion{% endblock %}

{% block body %}
    <h1>CtlTipoPromocion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctl_tipo_promocion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctl_tipo_promocion.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ ctl_tipo_promocion.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ctl_tipo_promocion_index') }}">back to list</a>

    <a href="{{ path('ctl_tipo_promocion_edit', {'id': ctl_tipo_promocion.id}) }}">edit</a>

    {{ include('ctl_tipo_promocion/_delete_form.html.twig') }}
{% endblock %}
