{% extends 'base.html.twig' %}

{% block title %}CtlTipoAviso{% endblock %}

{% block body %}
    <h1>CtlTipoAviso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctl_tipo_aviso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctl_tipo_aviso.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ ctl_tipo_aviso.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ctl_tipo_aviso_index') }}">back to list</a>

    <a href="{{ path('ctl_tipo_aviso_edit', {'id': ctl_tipo_aviso.id}) }}">edit</a>

    {{ include('ctl_tipo_aviso/_delete_form.html.twig') }}
{% endblock %}
