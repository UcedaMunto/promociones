{% extends 'base.html.twig' %}

{% block title %}Lista de avisos{% endblock %}

{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Activo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for promo_aviso in promo_avisos %}
            <tr>
                <td>{{ promo_aviso.id }}</td>
                <td>{{ promo_aviso.nombre }}</td>
                <td>{{ promo_aviso.descripcion }}</td>
                <td>{{ promo_aviso.activo ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('promo_aviso_show', {'id': promo_aviso.id}) }}"><i class="fa fa-eye" style="font-size:36px"></i></a>
                    <a href="{{ path('promo_aviso_edit', {'id': promo_aviso.id}) }}"><i class="fa fa-edit" style="font-size:36px"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('promo_aviso_new') }}"><i class="fa fa-plus" style="font-size:36px"></i></a>
{% endblock %}
