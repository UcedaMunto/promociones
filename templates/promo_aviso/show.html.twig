{% extends 'base.html.twig' %}

{% block title %}PromoAviso{% endblock %}

{% block body %}
    <h1>PromoAviso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo_aviso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ promo_aviso.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ promo_aviso.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ promo_aviso.activo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promo_aviso_index') }}">back to list</a>

    <a href="{{ path('promo_aviso_edit', {'id': promo_aviso.id}) }}">edit</a>

    {{ include('promo_aviso/_delete_form.html.twig') }}
{% endblock %}
