{% extends 'base.html.twig' %}

{% block title %}CtlTipoArchivo{% endblock %}

{% block body %}
    <h1>CtlTipoArchivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctl_tipo_archivo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ctl_tipo_archivo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ctl_tipo_archivo_index') }}">back to list</a>

    <a href="{{ path('ctl_tipo_archivo_edit', {'id': ctl_tipo_archivo.id}) }}">edit</a>

    {{ include('ctl_tipo_archivo/_delete_form.html.twig') }}
{% endblock %}
