<form method="post" action="{{ path('ctl_tipo_archivo_delete', {'id': ctl_tipo_archivo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ctl_tipo_archivo.id) }}">
    <button class="btn">Delete</button>
</form>
