{% extends 'base.html.twig' %}

{% block title %}DatosPersona2 index{% endblock %}

{% block body %}
    <h1>DatosPersona2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for datos_persona2 in datos_persona2s %}
            <tr>
                <td>{{ datos_persona2.id }}</td>
                <td>{{ datos_persona2.nombre }}</td>
                <td>
                    <a href="{{ path('datos_persona2_show', {'id': datos_persona2.id}) }}">show</a>
                    <a href="{{ path('datos_persona2_edit', {'id': datos_persona2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('datos_persona2_new') }}">Create new</a>
{% endblock %}
