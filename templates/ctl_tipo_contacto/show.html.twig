{% extends 'base.html.twig' %}

{% block title %}CtlTipoContacto{% endblock %}

{% block body %}
    <h1>CtlTipoContacto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctl_tipo_contacto.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ctl_tipo_contacto_index') }}">back to list</a>

    <a href="{{ path('ctl_tipo_contacto_edit', {'id': ctl_tipo_contacto.id}) }}">edit</a>

    {{ include('ctl_tipo_contacto/_delete_form.html.twig') }}
{% endblock %}
