<form method="post" action="{{ path('promo_contacto_establecimiento_delete', {'id': promo_contacto_establecimiento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promo_contacto_establecimiento.id) }}">
    <button class="btn">Delete</button>
</form>
